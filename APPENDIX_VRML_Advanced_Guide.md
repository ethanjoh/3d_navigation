# VRML 구조 익히기와 3차원 객체 제작
> **VRML의 안쪽 들여다보기: 표준 명세 기반의 3D 가상 공간 구현과 설계**

---

## 1. 서론
요즘은 쓸만한 HTML 문서 작성 툴이 많이 나와 사용자들을 유혹하고 있습니다. 하지만 여전히 텍스트 에디터 하나만 가지고도 얼마든지 좋은 페이지를 만들 수 있다는 사실은 분명 HTML의 가장 큰 매력 중 하나입니다.

**VRML(Virtual Reality Modeling Language)** 역시 마찬가지입니다. 텍스트 에디터만 있으면 3차원 페이지를 완벽하게 빌드할 수 있습니다. 공간 감각과 시각 디자인에 자신 있는 사용자(어린 시절 레고 블록을 조립하며 놀던 감각 정도면 충분합니다)라면 꼭 도전해 볼 만한 일입니다. 필요한 준비물은 VRML이 대체 어떻게 구성되어 있고, 또한 어떻게 설계해야 하는지 기초적인 작동 원리를 파악하는 것뿐입니다. 

본 가이드에서는 현재 널리 쓰이는 표준 규격인 **VRML 1.0**을 기준으로 가상 3D 월드의 작동 원리와 객체 제작 실무를 분석해 보겠습니다.

### 입문에 앞서 필요한 그래픽스 기초 지식
VRML은 컴퓨터 그래픽스 기술과 직결되어 있으므로, 3차원 공간을 정의하기 위한 전문적인 개념들의 이해가 필요합니다.
* 2차원 및 3차원 공간 벡터(Vector)
* 변환 행렬(Transformation Matrix)
* 부분(Local) 좌표계와 전체(World) 좌표계 간의 변환 구조
* 계층적 객체 구조(Hierarchical Object Structure) 및 RGB 컬러 배합 원리

이러한 개념들에 익숙하지 않다면 3차원 그래픽 관련 기초 서적을 가볍게 참조해 두는 것이 좋습니다. 또한 가변 구조와 블록 단위 컴포넌트(`{}`) 처리가 잦아 프로그래밍 언어(C, Pascal 등)의 경험이나 혹은 POV-Ray 등 과거의 스크립트 기반 3D 그래픽 언어 경험이 있다면 매우 빠른 속도로 적응할 수 있습니다.

---

## 2. VRML 파일의 정형 구조 및 구별 방법

VRML 파일은 기본적으로 순수한 텍스트(ASCII) 파일입니다. 따라서 메모장이나 문서 작성기로 즉시 열어서 읽고 편집할 수 있습니다.

```wrl
#VRML V1.0 ascii
# (이곳은 주석 처리 구역입니다. 1.0 규격을 선언하는 파일 식별용 헤더 행입니다)

Separator {
    DirectionalLight {
        direction 0 0 -1
    }
    PerspectiveCamera {
        position 0 0 5
    }
    # 빨간색 정육면체를 렌더링하는 영역
    Material {
        diffuseColor 1 0 0
    }
    Cube {}
}
```

### 2.1. 파일 헤더 선언 (File Header)
모든 VRML 파일의 최상단 첫 번째 행은 반드시 아래와 같은 식별 헤더로 시작되어야 합니다.
```wrl
#VRML V1.0 ascii
```
이 문장은 해당 파일이 VRML 규격을 준수하는 소스 코드임을 웹 브라우저와 렌더링 엔진에 알리는 고유 인식 코드입니다. 파일 시스템 상에서는 보통 `.wrl` 확장자를 사용하며 웹상에서도 이 마임 타입을 감지하지만, 아무리 확장자가 `.wrl`이어도 **첫 번째 줄에 이 선언이 누락되면 브라우저는 해당 파일을 VRML 씬으로 해석하지 못하고 무시**합니다.

### 2.2. 주석(Comment) 처리 규칙
VRML에서는 샵(**`#`**) 기호를 사용하여 주석을 표시합니다. 주석 선언문 이후로 동일 행에 기재되는 모든 텍스트는 브라우저 컴파일 엔진이 해석하지 않고 건너뜁니다. 주석이 다중 행으로 이어질 때는 줄이 바뀔 때마다 매번 `#` 기호를 맨 앞에 기입해 주어야 합니다.

---

## 3. VRML의 기본 문법: 노드(Node)와 필드(Field)

VRML 소스코드는 크게 노드와 필드의 결합 구조로 이루어져 있습니다.

### 3.1. 노드 (Node)
```wrl
명령어_노드타입 { 
    ... 
}
```
위와 같이 `노드이름 { ... }` 형태로 중괄호를 열고 닫는 하나의 명령 단위를 **노드(Node)**라고 부릅니다. 노드는 VRML을 구성하는 물리적이자 개념적인 가장 기본적 구조 단위입니다. 3차원 공간 내부의 실제 기하학적 형상(Box, Sphere 등), 카메라 뷰포인트, 광원(Light), 표면 컬러 및 텍스처 재질, 그리고 다른 페이지로 이동하는 하이퍼링크 등이 모두 개별 노드로 표현됩니다.

### 3.2. 필드 (Field)
노드 내부에 단독으로 기재되는 하위 설정값들을 **필드(Field)**라고 합니다. 이는 노드의 성질과 렌더링 매개변수를 조정하는 수치나 명세들을 전달하는 속성 필터입니다.
```wrl
# 노드 내부에서 가로, 세로, 깊이를 조절하는 필드 지정 예시
Cube {
    width  0.1
    height 0.2
    depth  4.0
}
```
필드는 `필드이름 필드값`으로 매핑됩니다. 만약 필드 선언을 생략하고 빈 노드 구조(`Cube {}`)로만 선언하면, 엔진 내부에 설계된 **기본 속성값(Default Value)**이 자동으로 주입되어 각 변의 길이가 `1.0`인 디폴트 정육면체가 씬에 나타나게 됩니다.

### 3.3. 부모(Parent)와 자식(Child)의 계층 구조
노드는 다른 노드를 얼마든지 내부에 포함할 수 있습니다.
* **부모 노드(Parent Node)**: 다른 노드를 품고 있는 구조체 노드
* **자식 노드(Child Node)**: 상위 노드의 내포 영역에 위치하는 종속 노드

이러한 부속 관계 처리에 사용되는 가장 대표적인 그루핑 노드가 **`Separator {}`**입니다. 부모 노드의 좌표나 속성 변화가 자식 노드들에게 순차적으로 상속 전파되는 트리 구조를 보장하기 위해, VRML 파일의 최상단 루트 노드는 반드시 **하나의 독립적인 `Separator {}` 노드**로 문을 열어야 한다는 설계 규칙이 존재합니다.

---

## 4. 필드의 수치 체계 (Field Types)

VRML은 3차원 수학적 공간의 연산을 지원하기 위해 11가지의 **단일 필드(SF - Single Field)** 타입과 4가지의 **다중 필드(MF - Multiple Field)** 타입을 지원합니다. 아무리 내부 파라미터 개수가 많아도(예: 4x4 행렬의 16개 데이터) 그것이 하나의 의미 구조를 형성한다면 SF(단일 필드)로 간주하며, 동일 형식 데이터가 대용량 포인트 집합 형태로 밀집 구성되는 경우에만 MF(다중 필드) 형식을 사용합니다.

### 4.1. 단일 필드 타입 (SF - Single Field)

* **SFBitMask**: 특정 비트 플래그의 상태(ON/OFF)를 Bitwise-OR 연산자로 한 번에 설정하는 필드
* **SFBool**: `TRUE` 또는 `FALSE` 두 가지 중 하나의 진리값을 담는 필드
* **SFColor**: RGB 광원의 강도를 나타내는 0.0에서 1.0 사이의 실수 3개로 배합된 필드 (예: `1 0 0`은 완전한 빨간색)
* **SFEnum**: 열거형 타입으로, 사전에 순차적으로 상수로 번호 정의된 식별값 지정 시 사용
* **SFFloat**: 단정도 실수(Floating-point) 값을 갖는 소수점 수치 필드
* **SFImage**: 2차원 비트맵 그림을 직접 코드 안에 담는 형태 (파일 비대화의 원인이 되므로 특수한 경우가 아니면 외부 이미지 리소스 링크 권장)
* **SFLong**: 32비트 부호 있는 정수 필드
* **SFMatrix**: 3D 그래픽 회전 행렬 연산 등에 대입되는 4x4의 3차원 변환 매트릭스 필드 (실수 16개로 구성)
* **SFRotation**: 특정 중심 회전축 기준의 회전을 묘사하는 실수 4개 조합 필드. 앞선 3개의 실수는 x, y, z 회전 기둥축을 설정하고 마지막 수치는 회전할 각도를 호도법(Radian)으로 규정합니다.
  ```wrl
  # Y축을 축으로 180도 회전시키는 예시 (라디안 값: 3.141592)
  rotation 0 1 0 3.141592
  ```
* **SFVec2f**: 2차원 좌표 표현을 위한 실수 2개 조합 벡터
* **SFVec3f**: 3차원 기하 이동 공간의 지점을 나타내는 실수 3개로 배합된 벡터 필드
* **SFString**: 큰따옴표(`" "`)로 둘러싸인 아스키 문자열 데이터 필드

### 4.2. 다중 필드 타입 (MF - Multiple Field)
다중 필드는 값 집합 전체를 대괄호(`[ ]`)로 묶고 원소들을 쉼표(`,`)로 나열하여 선언합니다.

* **MFColor**: 다중 RGB 색상 원소의 배열
* **MFLong**: 32비트 다중 정수값 배열
* **MFVec2f**: 2D 좌표 벡터들의 집합
* **MFVec3f**: 폴리곤 외형 정점 등을 저장하는 3D 공간 상 좌표 벡터들의 집합
  ```wrl
  # 다중 3D 정점 선언 예시
  point [ 0 0 0, 1 0 0, 0 1 0 ]
  ```

---

## 5. 노드의 종류별 상세 분류

VRML 세계는 크게 **형태 노드**, **속성 노드**, **그룹 노드** 삼각 구도의 노드 집합체로 가상 공간을 구체화합니다.

```
                  ┌─────────────── 루트 Separator ───────────────┐
                  │                                              │
         ┌────────┴────────┐                            ┌────────┴────────┐
         │     그룹 노드     │                            │    속성/형태 노드  │
         │  (Separator {}) │                            │  (Transform {}) │
         └────────┬────────┘                            └────────┬────────┘
                  │                                              │
         ┌────────┴────────┐                            ┌────────┴────────┐
         │     속성 노드     │                            │     형태 노드    │
         │   (Material {}) │                            │     (Cube {})   │
         └─────────────────┘                            └─────────────────┘
```

| 대분류 | 세부 그룹 | 관련 노드 리스트 | 핵심 기능 및 개요 |
| :--- | :--- | :--- | :--- |
| **형태 노드**<br>*(Geometry Nodes)* | 기본 도형 | `Cone`, `Cube`, `Cylinder`, `Sphere` | 가장 원초적인 부피 기하 객체를 생성하는 기본도형 노드군 |
| | 정점 폴리곤 | `PointSet`, `IndexedLineSet`, `IndexedFaceSet` | 점, 선, 면의 3D 좌표 데이터를 활용하여 독자적인 사용자 정의 폴리곤 외형 설계 |
| | 가상 텍스트 | `AsciiText` | 3차원 가상공간 안에 3D 문자 정보를 표출하고 정렬 |
| **속성 노드**<br>*(Property Nodes)* | 변환행렬 | `Translation`, `Rotation`, `Scale`, `Transform`, `MatrixTransform` | 가상공간 상에서 물체들의 이동 배치 좌표, 각도 방향, 크기 축척 비율을 정밀 조정 |
| | 표면 재질 | `Material`, `MaterialBinding`, `Texture2`, `Texture2Transform`, `TextureCoordinate2` | 물체 표면의 컬러 반사광, 투명도 지수 지정 및 2D 텍스처 맵 이미지의 변형 맵핑을 가이드 |
| | 연산 가속 | `ShapeHints`, `Coordinate3`, `Normal`, `NormalBinding` | 폴리곤 데이터 렌더링 시 법선 벡터 계산 기준을 조율하여 그래픽 가속에 기여 |
| | 카메라/광원 | `PerspectiveCamera`, `OrthographicCamera`, `DirectionalLight`, `PointLight`, `SpotLight` | 렌즈 관점의 연출 및 씬에 조명을 부착하여 입체 명암과 하이라이트 효과 발생 |
| **그룹 노드**<br>*(Group Nodes)* | - | `Group`, `Switch`, `Separator`, `TransformSeparator`, `WWWAnchor` | 다수의 노드를 하나의 컴포넌트로 병합 격리하거나, 실시간 렌더링 변형 상태 제어 및 하이퍼링크 라우팅 처리 |
| **외부 연동 노드** | - | `WWWInline` | 외부 네트워크에 올라가 있는 가상 파일을 실시간 호출해 현재 장면에 임베디드 |

---

## 6. 실전 개발 가이드: 탁자(Table) 제작 및 씬 배치

기본 구조에 대한 이론 파악이 끝났다면 직접 작은 탁자 모델을 3D 기하 연산 방식으로 조립한 후 가상 공간 레이아웃에 배치하는 실무 실습을 해보겠습니다.

### 6.1. 단일 탁자 모델 설계 (`Table.wrl`)
탁자는 가로세로 `1.0`, 높이 `0.5` 규격으로 설계합니다. 두께 `0.2`인 상판과 사각 코너 구석에 배치될 네 개의 가상 다리 기둥(`Cylinder`)을 변환 노드로 결합합니다.

* **좌표계 가이드**: VRML은 Y축이 수직 윗방향인 우수(오른손) 좌표계를 기저로 사용합니다.

```wrl
#VRML V1.0 ascii
# 3D 입체 탁자 디자인 모델

Separator {
    # 1. 탁자 상판 디자인 (1.0 x 0.2 x 1.0)
    Separator {
        Material {
            diffuseColor 0.6 0.4 0.2 # 브라운 목재 톤 색상 배합
        }
        Cube {
            width  1.0
            height 0.2
            depth  1.0
        }
    }

    # 2. 탁자 다리 다리-1 (Y축 아래로 이동 후 구석 배치)
    Separator {
        Material {
            diffuseColor 0.8 0.8 0.8 # 금속 실버 다리
        }
        Translation {
            translation 0.4 -0.3 0.4
        }
        Cylinder {
            radius 0.05
            height 0.4
        }
    }

    # 3. 탁자 다리 다리-2
    Separator {
        Material {
            diffuseColor 0.8 0.8 0.8
        }
        Translation {
            translation -0.4 -0.3 0.4
        }
        Cylinder {
            radius 0.05
            height 0.4
        }
    }

    # 4. 탁자 다리 다리-3
    Separator {
        Material {
            diffuseColor 0.8 0.8 0.8
        }
        Translation {
            translation 0.4 -0.3 -0.4
        }
        Cylinder {
            radius 0.05
            height 0.4
        }
    }

    # 5. 탁자 다리 다리-4
    Separator {
        Material {
            diffuseColor 0.8 0.8 0.8
        }
        Translation {
            translation -0.4 -0.3 -0.4
        }
        Cylinder {
            radius 0.05
            height 0.4
        }
    }
}
```

### 6.2. 완성된 물체의 마스터 월드 내 공간 배치
상판 조립이 마쳐진 탁자 객체를 전체 풍경을 묘사하는 마스터 씬의 한구석에 비스듬하게 회전시켜 어색하지 않게 배치해 보겠습니다.
```wrl
# 마스터 씬 파일 내부 배치 구문 예시
Separator {
    # ... 기존 방 인테리어 모델 로딩 구조 ...

    # 기구축된 탁자 인스턴스를 월드 X:0.8, Y:0.5, Z:0.8 지점에 배치 후 Y축 기준 비스듬히 40도 회전
    Separator {
        Transform {
            translation 0.8 0.5 0.8
            rotation    0 1 0 0.7  # Y축 회전 0.7 라디안 적용
        }
        
        # 외부 혹은 기정의된 탁자 객체 노드 탑재
        # (WWWInline 이나 DEF/USE 참조 모델 연동 가능)
    }
}
```

---

## 7. 관련 참고 리소스 자료실

VRML 명세에 대한 심층 기술 문서 및 FAQ 데이터 아카이빙은 아래의 주요 리서치 포털에서 보다 구체적으로 만나보실 수 있습니다.

* **SDSC VRML 정보 파트너스 허브**: [http://rosebud.sdsc.edu/SDSC/Partners/vrml](http://rosebud.sdsc.edu/SDSC/Partners/vrml)
* **VRML Repository & FAQ 포털**: [http://www.web3d.org](http://www.web3d.org) (구 VRML 컨소시엄 공식 기술 협회 채널)
